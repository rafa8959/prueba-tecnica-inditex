spring.application.name=price

server.port=8080

# --- Swagger ---
springdoc.swagger-ui.url=/price-api.yaml
springdoc.swagger-ui.path=/swagger-ui.html

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=none
spring.sql.init.mode=always